
public class Day5 {
    public static void solve(String puzzleInput) {
        // StringBuilder word = new StringBuilder();
        // int niceWords = 0;
        // for (char c : puzzleInput.toCharArray()) {
            // if (c == '\r') continue;
            // else if (c == '\n') {
                // if (isNice1(word.toString())) niceWords++;
                // word.delete(0, word.length());
            // }
            // else word.append(c);
        // }

        // System.out.println("Nice words: " + niceWords);
    }

    // public static boolean isNice1(String input) {
        // boolean doubleLetter = false;

        // char previousChar = ' ';
        // int vowelCount = 0;
        // for (char c : input.toCharArray()) {
            // if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') vowelCount++;
            // if (c == previousChar) doubleLetter = true;

            // // Check for naughty substrings
            // String lastTwo = previousChar + "" + c;
            // if (lastTwo.equals("ab") || lastTwo.equals("cd") || lastTwo.equals("pq") || lastTwo.equals("xy")) return false;

            // previousChar = c;
        // }

        // return vowelCount >= 3 && doubleLetter;
    // }

    // public static boolean isNice2(String input) {
        // boolean doublePairs = false;
        // HashSet<>
    // }
}
